const ObjectType = {
    DOOR: 'door',
    PLATE: 'plate',
}

const ImageStatus = {
    PROCESSING: 'processing',
    READY: 'ready',
    ERROR: 'error',
}

const FeatureName = {
    COLOR: 'color',
    TEXT: 'text',
}

module.exports = {
    ObjectType,
    ImageStatus,
    FeatureName,
}